# Required
DATABASE_URL=postgres://user:password@host:5432/dbname
JWT_SECRET=replace-with-a-long-random-string

# Optional — runs Tier-2 contract audits through Claude instead of the
# built-in rule-based clause-matcher when set.
ANTHROPIC_API_KEY=

# Local dev only — Vercel provides its own port binding.
PORT=3000
